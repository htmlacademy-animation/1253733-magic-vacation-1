import React from "react";

import styles from "./Styles.module.scss";

const NotFound = () => {
  return (
    <div className={styles.root}>
      <h1 className={styles.addTitle}>Ничего не найдено(:</h1>
    </div>
  );
};

export default NotFound;
